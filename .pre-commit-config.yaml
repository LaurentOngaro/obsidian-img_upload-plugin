repos:
  - repo: local
    hooks:
      - id: run-ci-checks
        name: Run CI checks (tests + build)
        entry: npm run ci:checks
        language: system
        pass_filenames: false
        require_serial: true
